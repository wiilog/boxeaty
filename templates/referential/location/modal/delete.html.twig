<div id="modal-delete-location" class="modal fade delete">
    <div class="modal-dialog">
        <div class="modal-content">
            <button type="button" class="close-modal" data-bs-dismiss="modal" aria-label="Close"></button>
            <div class="delete-image"></div>
            <h1>
                {% if location.outClient or location.boxes | length > 0 or location.boxRecords | length > 0 %}
                    Cet emplacement est l'emplacement de sortie d'un client ou est lié à un(e) ou plusieurs Box, mouvements.<br>
                    Vous ne pouvez pas le supprimer.<br>
                    Voulez-vous le désactiver ?
                {% else %}
                    Voulez-vous vraiment supprimer cet emplacement ?
                {% endif %}
            </h1>
            <input type="hidden" name="id" class="data">

            <div class="footer">
                <button type="button" class="secondary" data-bs-dismiss="modal">Annuler</button>
                <button type="submit" class="primary">{{ (location.boxes | length > 0 or location.boxRecords | length > 0) ? 'Désactiver' : 'Supprimer' }}</button>
            </div>
        </div>
    </div>
</div>
